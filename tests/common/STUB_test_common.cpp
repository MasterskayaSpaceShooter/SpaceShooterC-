#include <STUB_class.hpp>  // Модуль common теперь доступен напрямую по имени!
#include <gtest/gtest.h>

TEST(CommonModuleTest, IntegrityCheck) {
    // В будущем здесь будет тест общих классов
    SUCCEED();
}